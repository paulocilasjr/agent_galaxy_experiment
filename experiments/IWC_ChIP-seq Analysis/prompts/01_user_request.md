User requested to:
1) run the local `chipseq-pe.ga` workflow,
2) compare final results against the previously completed Galaxy workflow run,
3) generate artifacts with structure similar to other project examples,
4) restart from the beginning of this experiment and document through current state.
