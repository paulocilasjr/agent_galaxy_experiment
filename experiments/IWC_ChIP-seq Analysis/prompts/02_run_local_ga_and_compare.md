Run the local workflow file `IWC_ChIP-seq Analysis/chipseq-pe.ga` from scratch in Galaxy,
then compare final outputs against the previous completed baseline run (IWC v1.0),
and document whether final results are equivalent.
